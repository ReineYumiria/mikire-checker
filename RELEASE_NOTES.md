# Release Notes

## v1.20.0（2026-06-27）

見切れチェッカー v1.20.0 では、AdSense 審査での「有用性の低いコンテンツ」判定対策として、ツール本体に加えて使い方・説明・ガイド・FAQを備えた情報価値のあるWebツールサイトへ拡充しました。

### 追加・更新内容

- **トップページ下部の説明セクションを拡充**
  - 見切れチェッカーでできること、使い方3ステップ、対応プリセットの考え方、FAQを追加
  - 画像はサーバーへ送信されず、ブラウザ内で処理される設計であることを明記
  - YouTube、Twitter、Discordなどの見切れ確認に役立つ導線を追加
- **情報ページを拡充**
  - `/about` にツールの目的、想定ユーザー、登録不要で使えること、表示確認・安全領域確認・PNG書き出し機能の説明を追加
  - `/how-to-use` にアップロードからPNG書き出しまでの手順と、うまく表示されない場合の確認ポイントを追加
  - `/contact` を追加し、問い合わせ・不具合報告・要望受付の案内と、フォーム準備中である旨を記載
- **ガイドページを追加**
  - `/guides/youtube-thumbnail-safe-area`
  - `/guides/twitter-header-safe-area`
  - `/guides/discord-profile-image`
- **SEO / クロール導線を更新**
  - `sitemap.ts` に新規ページを追加
  - トップページとフッターから新規ページへの内部リンクを追加
- **バージョン更新**
  - `package.json` / `package-lock.json` の version を `1.20.0` に更新

### 補足

- Canvas描画処理、画像アップロード処理、PNG書き出し処理、プリセット定義は変更していません。
- 広告コードや広告枠の大きな変更は行っていません。


## v1.19.2（2026-06-11）

見切れチェッカー v1.19.2 では、スマホ二本指回転対応とスマホ表示のプレビュー位置改善を行いました。

### 追加・更新内容

- **スマホ二本指回転ジェスチャー対応**
  - スマホ / タッチ操作で二本指による回転ジェスチャーに対応しました
  - 二本指操作中は、移動・ピンチズーム・回転を同時に扱えるようになりました
  - 二本指回転は、回転スライダー / 回転角度入力の表示と連動します
  - 回転角度は v1.19.1 と同じく -180°〜180° の範囲で自然にループします
- **スマホ表示のプレビュー位置改善**
  - 操作パネルを「操作」と「調整・書き出し」の2つに分割しました
  - スマホ表示では、回転操作の直後にプレビューが来るように並び順を変更しました
  - 反転・安全領域ガイド・保存方法・PNG書き出しは、スマホ表示ではプレビューの下に配置されます
  - PC表示では、中央のプレビューと右サイドバーの位置は維持しつつ、左列を上下2枚のカードに整理しました

### 補足

- 既存の1本指ドラッグ、通常ホイールズーム、Shift+ホイール回転、回転スライダー / 回転角度入力、PNG書き出し、円形表示プレビュー、YouTubeカード風プレビューは変更なく利用できます
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。

## v1.19.1（2026-06-11）

見切れチェッカー v1.19.1 では、Shift + ホイール回転角度のループ修正を行いました。

### 修正内容

- **Shift + ホイール回転角度のループ修正**
  - 変更前：Shift + ホイール回転を続けると、rotation の値が 180° を超えて 183°、186°…のように増え続けていました
  - 変更後：rotation を -180°〜180° の範囲に正規化するようにし、180°を超えた場合は -177° 側へ、-180°を下回った場合は 177° 側へ回り込むようにしました（回転スライダーと同じ正規化表示）
  - `normalizeAngle` を `src/lib/canvas.ts` の共通ヘルパーとして切り出し、`ControlPanel` 側でも同じヘルパーを使うよう整理しました

### 補足

- offsetX / offsetY は従来通り、実際に回転した差分角度（normalize前のdelta）を使って `rotateOffset` で更新しているため、v1.19.0 で統一した Canvas / プレビュー中央支点の挙動は維持されています
- 回転スライダー / 回転角度入力の挙動は変更なく、270 / 360 / -270 / -360 などの角度入力も引き続き使用できます
- 通常ホイールズーム、ドラッグ、回転・反転、PNG書き出し、ガイド付きPNG書き出し、円形表示プレビュー、YouTubeカード風プレビューは変更なし
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。

## v1.19.0（2026-06-11）

見切れチェッカー v1.19.0 では、回転操作の支点をCanvas / プレビュー中央に統一しました。

### 修正内容

- **回転操作の支点を統一**
  - 変更前：回転スライダー / 回転角度入力で回転すると、画像中心を支点に回って見えていました（Shift + ホイール回転は v1.16.0 で Canvas 中央を支点にした挙動に調整済み）
  - 変更後：回転スライダー / 回転角度入力でも、rotation の変更差分に合わせて offsetX / offsetY を回転させるようにし、Canvas / プレビュー中央を支点に回転するようになりました
  - Shift + ホイール回転・回転スライダー・回転角度入力のいずれを使っても、見え方が揃います
  - Canvas表示・PNG書き出し・ガイド付きPNG書き出し・円形表示プレビュー・YouTubeカード風プレビューでも同じ挙動になります
- Shift + ホイール回転は、共通ヘルパー `rotateOffset` を使うよう内部整理（挙動は変更なし）

### 補足

- rotation は v1.15.0 以降の連続値運用を維持しており、270 / 360 / -270 / -360 などの角度入力も引き続き使用できます
- 回転スライダーは -180〜180 の正規化表示を維持しつつ、最短角度差で連続回転値へ変換するようにしたため、359°→0°のような操作で大きくジャンプすることはありません
- 回転・反転後のドラッグ方向（v1.16.0で対応）、左右反転 / 上下反転、通常ホイールズーム、ズーム数値入力、PNG書き出し、保存方法選択、円形表示プレビュー、YouTubeカード風プレビュー、カテゴリ / 対象サービス / プリセットの3段階選択UIは変更なし
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。

## v1.18.1（2026-06-08）

見切れチェッカー v1.18.1 では、音楽カテゴリに Spotify 向けプリセットを追加しました。

### 追加内容

- **Spotify** プリセットを追加（カテゴリ：音楽）
  - Artist Header（2660×1140）
  - Image Gallery（690×500）
  - Cover Art（3000×3000）
- `serviceCategories` に `Spotify: "音楽"` を追加
- カテゴリ / 対象サービス / プリセットの3段階選択UIで「音楽 → Spotify」が選択可能になりました

### 補足

- Canvas 描画・画像アップロード・ドラッグ・ズーム・回転・反転・PNG 書き出し・保存方法選択・円形表示プレビュー・YouTube カード風プレビューは変更なし。
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。

## v1.18.0（2026-06-08）

見切れチェッカー v1.18.0 では、新規サービスのプリセット追加とカテゴリ構成の整理を行いました。

### 追加・変更内容

- **Threads** プリセットを追加（カテゴリ：SNS）
  - プロフィール画像（320×320、円形ガイド）
  - 投稿画像 正方形（1080×1080）
  - 投稿画像 縦長 4:5（1080×1350）
- **Slack** プリセットを追加（カテゴリ：コミュニケーション）
  - ワークスペースアイコン（512×512、円形ガイド）
  - プロフィール画像（512×512、円形ガイド）
- **ニコニコ動画** プリセットを追加（カテゴリ：動画 / 配信）
  - 動画サムネイル（1280×720）
- **「コミュニケーション」カテゴリ**を新設（SNS と動画 / 配信の間に配置）
  - Discord をこのカテゴリに移動（旧：開発 / その他）
  - 新規追加の Slack もこのカテゴリに追加

### 補足

- Canvas 描画・画像アップロード・ドラッグ・ズーム・回転・反転・PNG 書き出し・保存方法選択・円形表示プレビュー・YouTube カード風プレビューは変更なし。
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。

## v1.17.0（2026-06-08）

見切れチェッカー v1.17.0 では、対応サービスの増加に伴いサービス選択UIを整理しました。

### 変更内容

- **サービス選択UIを「カテゴリ / 対象サービス / プリセット」の3段階に分割**
  - 変更前：対象サービスの select 内にカテゴリ見出しと各サービスが混在し、サービス追加とともに縦長になっていました
  - 変更後：操作パネルの選択UIを以下の3段階に分割しました
    1. **カテゴリ** — SNS / 動画・配信 / 創作・投稿 / クリエイター支援・ファンコミュニティ / 音楽 / ゲーム・ストア / 開発・その他
    2. **対象サービス** — 選択中カテゴリに属するサービスのみ表示
    3. **プリセット** — 選択中サービスに属するプリセットのみ表示（従来通り）
- カテゴリを変更すると、そのカテゴリ内の先頭サービスと先頭プリセットへ自動的に切り替わります
- サービスを変更すると、そのサービス内の先頭プリセットへ自動的に切り替わります
- 既存の標準 select を使用しており、カスタムコンポーネント化はしていません

### 補足

- Canvas 描画・画像アップロード・ドラッグ・ズーム・回転・反転・PNG 書き出し・保存方法選択・円形表示プレビュー・YouTube カード風プレビューは変更なし。
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。

## v1.16.0（2026-06-08）

見切れチェッカー v1.16.0 では、v1.15.0 で追加した回転・左右反転・上下反転機能について、ドラッグ操作の挙動を改善しました。

### 修正内容

- **回転・反転後のドラッグ操作を改善**
  - 回転・左右反転・上下反転を行った状態でも、ドラッグ移動の方向が画面座標基準で自然に動くよう修正しました
  - 左右反転 ON の状態で右へドラッグすると、見た目上右へ動くようになりました
  - 上下反転 ON の状態で上へドラッグすると、見た目上上へ動くようになりました
  - 回転状態でも、ドラッグ方向が回転後の座標系に引っ張られにくくなりました
  - Canvas 表示・PNG 書き出し・ガイド付きPNG書き出し・円形表示プレビュー・YouTube カード風プレビューで描画挙動を揃えました
- **Shift + ホイール回転で Canvas 中央を支点とした回転**を維持
  - 回転と同時にオフセットも更新することで、画像が Canvas 中央を支点に軌道を描くよう実装
  - offset=0 の状態での回転は引き続き Canvas 中央で回転します

### 補足

- v1.15.0 で追加した画像回転・左右反転・上下反転・ズーム数値入力欄・Shift + ホイール回転は引き続き利用できます。
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。
- ズーム・ドラッグ・ピンチズーム・PNG 書き出し・保存方法選択は変更なし。

## v1.14.0（2026-06-07）

見切れチェッカー v1.14.0 では、円形プロフィール画像・アイコン系プリセット向けの円形表示プレビュー追加と、pixiv / Mastodon プリセットの追加を行いました。

### 追加・更新内容

- **円形表示プレビュー**を追加（`CircleIconPreview` コンポーネント）
  - `guideShape: "circle"` が設定されているプリセット選択時に、Canvas 付近に円形表示プレビューを自動表示
  - サービス名の固定判定はせず、今後 `guideShape: "circle"` を追加したプリセットも自動で対象になる構造
  - 既存の Canvas 操作（ズーム・ドラッグ・ピンチズーム）の結果をリアルタイムで反映
  - 画像未アップロード時はダークグレーのプレースホルダー円を表示
  - PNG 書き出し・ガイド付きPNG書き出しには影響なし
- **pixiv プロフィール画像（暫定）** プリセットを追加（カテゴリ：創作 / 投稿）
  - サイズ：500×500（1:1）
  - pixiv 公式ヘルプでは最大ファイルサイズが 5MB と案内されていますが、明確な推奨ピクセル数が確認しづらいため、500×500 の暫定プリセットとして追加
  - `guideShape: "circle"` を設定しており、円形表示プレビューの対象
- **Mastodon Avatar** プリセットを追加（カテゴリ：SNS）
  - サイズ：400×400（1:1）
  - Mastodon 公式ドキュメントで、アバター画像は 400×400px に縮小されることが案内されています
  - `guideShape: "circle"` を設定しており、円形表示プレビューの対象
- **Mastodon Header** プリセットを追加（カテゴリ：SNS）
  - サイズ：1500×500（3:1）
  - Mastodon 公式ドキュメントで、ヘッダー画像は 1500×500px に縮小されることが案内されています
  - 通常の矩形プリセットとして追加（円形表示プレビュー対象外）

### 補足

- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。
- Canvas 描画・ズーム・ドラッグ・ピンチズーム・PNG 書き出し・保存方法選択は変更なし。

## v1.13.0（2026-06-07）

見切れチェッカー v1.13.0 では、テスターからの追加対応サイト案をもとに、SNS / クリエイター支援 / ファンコミュニティ系プリセットを追加しました。

### 追加・更新内容

- **Facebook** プリセットを追加（カテゴリ：SNS）
  - ページカバー：851×315
  - プロフィール画像：320×320（既存の円形ガイドを使用）
- **Fantia** プリセットを追加（カテゴリ：クリエイター支援 / ファンコミュニティ）
  - アイキャッチ（暫定）：688×387
  - 横幅688px以上がきれいに表示されやすいという案内をもとに16:9目安として追加。高さ・比率は暫定のため名称に「暫定」を明記
- **Fansly** プリセットを追加（カテゴリ：クリエイター支援 / ファンコミュニティ）
  - ヘッダー：3261×1126
- **OnlyFans** プリセットを追加（カテゴリ：クリエイター支援 / ファンコミュニティ）
  - バナー（暫定）：1797×540
  - 複数のサイズ情報があるため暫定プリセットとして追加。名称に「暫定」を明記
- サービス選択に「クリエイター支援 / ファンコミュニティ」カテゴリを新設（「創作 / 投稿」と「音楽」の間に配置）

### 補足

- ニジエは公式または実画面でのサイズ確認ができ次第、別バージョンで追加予定です。
- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。
- Canvas 描画・ズーム・ドラッグ・ピンチズーム・PNG 書き出し・保存方法選択は変更なし。

## v1.12.0（2026-06-07）

見切れチェッカー v1.12.0 では、友人テスターからのフィードバックをもとに、PNG書き出し時の保存方法を選択できるようにしました。

### 追加・更新内容

- 操作パネルの「PNGで書き出し」ボタン付近に、**保存方法の選択UI**（ラジオボタン）を追加
  - **通常ダウンロード**（デフォルト）：従来通りの `<a download>` 相当の保存処理
  - **保存先を選んで保存**：対応ブラウザで `showSaveFilePicker()` を使用し、保存先ダイアログを表示
- 保存先を選んで保存の動作確認
  - Chrome / Edge：保存先ダイアログが表示され、指定したフォルダへ保存できることを確認済み
  - Firefox：`showSaveFilePicker` 非対応のため通常ダウンロードへフォールバックし、ボタン付近にその旨を表示
- 保存先選択ダイアログをキャンセルした場合は、エラー扱いにせず保存を中断します
- PNG書き出し・ガイド付きPNG書き出しの両方で、同じ保存方法が使われます
- 画像処理は引き続きブラウザ内で完結しています。サーバー保存・アップロード処理は追加していません

### 補足

- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。
- Canvas 描画・ズーム・ドラッグ・ピンチズーム・プリセット切り替えは変更なし。

## v1.11.0（2026-06-07）

見切れチェッカー v1.11.0 では、友人テスターからのフィードバックをもとに VRChat 向けプリセットを追加しました。

### 追加・更新内容

- サービス選択に **VRChat** を追加（カテゴリ：ゲーム / ストア）
- **VRChat World Thumbnail** プリセットを追加
  - サイズ：1200×900（4:3）
  - VRChat Wiki で案内されているワールドサムネイル寸法に基づいた公式準拠プリセットです
- **VRChat Group Banner（暫定）** プリセットを追加
  - サイズ：1920×1080（16:9）
  - VRChat 公式に明確なバナーサイズの案内が確認しづらいため、16:9 の目安として設定しています
  - 表示場所によってトリミングが変わる可能性があるため、プリセット名に「暫定」と明記しています

### 補足

- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。
- Canvas 描画・ズーム・ドラッグ・ピンチズーム・PNG 書き出し処理は変更なし。

## v1.10.0（2026-06-07）

見切れチェッカー v1.10.0 では、Canvas のズーム操作の挙動を改善しました。

### 追加・更新内容

- マウスホイールでズームしても、ページがスクロールしなくなりました
  - React の `onWheel` はパッシブイベントのため `preventDefault()` が効かない問題を修正
  - ネイティブ `addEventListener("wheel", handler, { passive: false })` を使用する方式に変更
  - `handleWheelRef` パターンで最新の state を参照できるよう実装
- ズームスライダーで拡大縮小する際、Canvas の中央を基点として画像位置が保たれるようになりました
  - 変更前：スライダー操作で offset がリセットされず、ズームと位置ズレが生じることがあった
  - 変更後：`handleZoomChange` で `offsetX / offsetY` をズーム比率に応じてスケールするよう修正

### 補足

- AdSense 審査はまだ完了していません。実広告は引き続き表示されません。
- ads.txt・実広告ユニット ID・Privacy Policy の「広告掲載中」表現への変更は審査完了後に対応予定です。
- `AdSlot` コンポーネント・ページ内広告枠の配置・Canvas 描画・PNG 書き出し処理は変更なし。

## v1.9.0（2026-06-06）

見切れチェッカー v1.9.0 では、Google AdSense のサイト登録・所有権確認・審査リクエストへの対応を行いました。

### 追加・更新内容

- `src/app/layout.tsx` に AdSense 所有権確認用 Script を追加
  - `next/script` を使用し `strategy="beforeInteractive"` で読み込む
  - AdSense client ID は `NEXT_PUBLIC_GOOGLE_ADSENSE_CLIENT` 環境変数から読む
  - 環境変数未設定時は Script を出力しない
- `metadata.other` に `google-adsense-account` meta タグを追加
  - `NEXT_PUBLIC_GOOGLE_ADSENSE_CLIENT` が設定されている場合のみ出力する（条件付きスプレッド）
- Vercel の Production 環境変数に `NEXT_PUBLIC_GOOGLE_ADSENSE_CLIENT` を設定
- AdSense のサイト所有権確認が完了
- AdSense の審査リクエストを実施（現在審査待ち）

### 補足

- 実広告ユニット ID・slot ID・ads.txt・実広告表示の有効化は未導入です。
- Privacy Policy の「広告掲載中」表現への変更はまだ行いません。
- 実広告の導入は AdSense 審査結果を確認してから **v1.10.0 以降**で対応予定です。
- `AdSlot` コンポーネント・ページ内広告枠の配置・Canvas 描画・画像処理・PNG 書き出し処理は変更なし。

## v1.8.0（2026-06-06）

見切れチェッカー v1.8.0 では、Google AdSense申請前の最終確認と実広告導入前の構成整理を行いました。

### 確認・整理内容

- v1.7.0 で追加した `AdSlot` 基盤の動作方針を確認
  - 環境変数未設定時は常にプレースホルダー表示になることを確認
  - 開発環境・本番環境ともに実広告は表示されないことを確認
- 広告枠の配置が操作UI・画像プレビュー・Canvas と誤認されにくい位置にあることを確認
- `/privacy` の広告・Cookie 表現が「現時点では広告未掲載・将来的に導入の可能性あり」という実態と整合していることを確認

### 方針

- 実広告導入（AdSense script 追加・ads.txt 作成・実広告スロット ID 設定）は **v1.9.0 以降**に分けて実施します
- v1.8.0 では AdSense script・ads.txt・実広告ユニット ID は未導入のまま

### 変更なし

- `AdSlot.tsx` / `AdPlaceholder.tsx` / `layout.tsx` / `page.tsx` / Canvas 関連は変更なし
- Privacy Policy は現状維持

## v1.7.0（2026-06-06）

見切れチェッカー v1.7.0 では、Google AdSense 実広告導入前の構成準備を行いました。

### 追加・更新内容

- `AdSlot` コンポーネントを新規追加（`src/components/AdSlot.tsx`）
  - 環境変数による広告 ON / OFF 設計を実装
  - 参照する環境変数：`NEXT_PUBLIC_ADSENSE_ENABLED` / `NEXT_PUBLIC_GOOGLE_ADSENSE_CLIENT` / `NEXT_PUBLIC_AD_SLOT_SIDEBAR` / `NEXT_PUBLIC_AD_SLOT_FOOTER`
  - 広告表示条件：本番環境 かつ `NEXT_PUBLIC_ADSENSE_ENABLED=true` かつ各変数が設定済みの場合のみ
  - 条件を満たさない場合は既存の `AdPlaceholder` を表示（開発環境・環境変数未設定時は常にプレースホルダー）
- `page.tsx` の広告枠を `AdPlaceholder` から `AdSlot` に差し替え
  - 右カラム下部広告枠：`slotId={process.env.NEXT_PUBLIC_AD_SLOT_SIDEBAR}`
  - ページ下部広告枠：`slotId={process.env.NEXT_PUBLIC_AD_SLOT_FOOTER}`

### 補足

- v1.7.0 では AdSense script・ads.txt・実広告スロット ID は未導入です。
- 環境変数を設定しない限り、デプロイ後も引き続きプレースホルダー表示になります。
- `AdPlaceholder` コンポーネントは変更・削除せず、`AdSlot` の内部フォールバックとして利用しています。
- v1.6 の SEO 基盤（sitemap / robots）および既存ツール機能には影響なし。
- Canvas 描画・画像処理・PNG 書き出し処理は変更なし。

## v1.6.0（2026-06-06）

見切れチェッカー v1.6.0 では、AdSense申請前・検索流入向けのSEO基盤整備を行いました。

### 追加・更新内容

- `src/app/sitemap.ts` 追加（`/sitemap.xml` 自動生成対応）
  - `/` `/about` `/how-to-use` `/privacy` の4URLを含む
- `src/app/robots.ts` 追加（`/robots.txt` 自動生成対応）
  - 全User-agentをAllow、sitemapのURLを明示
- トップページ下部に「見切れチェッカーとは」説明セクションを追加
  - ツール概要・対応サービス例・主な機能・ブラウザ内処理・関連ページ導線を掲載
  - A案広告枠とfooterの間に配置
- `/privacy` ページの metadata description を具体化

### 補足

- 実広告コード・Google AdSense 等の script は未導入です。広告枠はプレースホルダーのままです。
- `AdPlaceholder` コンポーネントは変更なし。
- Canvas 描画・画像処理・PNG 書き出し処理は変更なし。

## v1.5.0（2026-06-06）

見切れチェッカー v1.5.0 では、AdSense申請前のサイト内ページ整備とフッター導線の整理を行いました。

### 追加・更新内容

- About ページ追加（`/about`）
  - ツール概要・特徴・対応サービス一覧・作者・ライセンス・GitHubへの導線を掲載
- 使い方ページ追加（`/how-to-use`）
  - 画像アップロードからPNG書き出しまでの7ステップを掲載
- Privacy Policy ページ追加（`/privacy`）
  - サイト内で読めるプライバシーポリシーページを追加
  - 将来的にGoogle AdSense等の広告サービスを導入する可能性・Cookie使用の可能性を明記
- フッター導線を整理
  - About / 使い方 / プライバシーポリシー / GitHub へのリンクを追加
  - プライバシーポリシーのリンク先を GitHub raw リンクから `/privacy` に変更
  - 「画像はブラウザ内で処理されます」をリンク行の下に別行表示
- `PRIVACY.md` 更新
  - 「広告について」セクションを追加（広告・Cookie関連記述）
  - 将来的な広告掲載の可能性を明記

### 補足

- 実広告コード・Google AdSense 等の script は未導入です。
- `AdPlaceholder` コンポーネントは変更なし。
- 画像処理・Canvas 内部サイズ・描画処理・PNG 書き出し処理は変更なし。

## v1.4.0（2026-06-06）

見切れチェッカー v1.4.0 では、収益化準備として広告枠プレースホルダーを追加しました。

### 追加・更新内容

- 広告枠プレースホルダー用コンポーネント `AdPlaceholder` を新規作成
- 右カラム下部に広告枠プレースホルダーを追加
- ページ下部（footer 直前）に広告枠プレースホルダーを追加

### 補足

- 実広告コード・Google AdSense 等の script は未導入です。表示はプレースホルダーのみです。
- 広告枠は Canvas 操作・画像アップロード・ズーム・PNG 書き出し操作を邪魔しない位置に配置しています。PC / スマホ表示での干渉がないことを確認済みです。
- スマホ表示では右カラム下部広告枠とページ下部広告枠がフッター直前に縦並びになりますが、操作 UI から十分離れているため現状維持としています。
- 実広告導入後に重く見える場合は、右カラム下部広告枠を PC 専用表示（`lg:` 以上のみ表示）に変更する調整候補があります。
- 画像処理・Canvas 内部サイズ・描画処理・PNG 書き出し処理は変更なし。

## v1.3.0

見切れチェッカー v1.3.0 では、縦長プリセットのPC表示改善と、内部の不具合修正を行いました。

### 追加・更新内容

- 縦長プリセット選択時のCanvas表示が画面内に収まりやすくなるよう調整
  - Instagram ストーリーズ / リール、TikTok 動画カバー、Steam 縦型 / ライブラリカプセルなどが対象
  - Canvas内部サイズ・描画処理・PNG書き出し処理は変更なし
- YouTubeホーム風カードプレビューの lintエラー修正（`draw` 関数を `useCallback` で定義し直し）

### 補足

- 縦長プリセットのCanvas表示は、CSS表示サイズのみを調整しています。描画品質・書き出し結果・操作感に変化はありません。
- 対象サービス選択プルダウンの開閉リスト高さ制御は、標準 HTML select の仕様上 v1.3 では見送りました。将来候補として継続検討します。

## v1.2.0

見切れチェッカー v1.2.0 では、サービス選択UIのカテゴリ対応、Bluesky / Misskey プリセットの追加、サイト表示風プレビューの試作を行いました。

### 追加・更新内容

- サービス選択UIをカテゴリ別表示に変更（SNS / 動画・配信 / 創作・投稿 / 音楽 / ゲーム・ストア / 開発・その他）
- Bluesky プリセット追加
  - プロフィール画像
  - ヘッダー画像
  - 投稿画像（16:9）
- Misskey プリセット追加
  - アイコン（目安）
  - バナー（目安）
- YouTube サムネイルプリセット選択時に、ホーム風カードプレビューを試作追加

### 補足

- Misskey はインスタンスやテーマによって表示が異なる場合があるため、目安プリセットとして扱っています。
- Bluesky の投稿画像は複数の比率を扱えるサービスのため、16:9 は代表的な目安として設定しています。
- サイト表示風プレビュー（YouTube ホーム風）は試作機能のため、YouTube の実際の表示とは異なる場合があります。現在は YouTube サムネイルプリセット選択時のみ表示されます。

## v1.1.0

見切れチェッカー v1.1.0 では、対応サービス / 用途プリセットを大幅に追加しました。

### 追加・更新内容

- Steam プリセット追加
- SoundCloud プリセット追加
- Twitch プリセット追加
- pixiv プリセット追加
- FANBOX プリセット追加
- itch.io プリセット追加
- GitHub プリセット追加
- Instagram プリセット追加
- TikTok プリセット追加
- BOOTH プリセット追加
- Ci-en プリセット追加
- Skeb プリセット追加
- README を v1.1 対応内容に更新
- `npm run build` 成功
- `v1.1.0` tag 作成・push 完了

### 補足

- Instagram ストーリーズ / リール、TikTok 動画カバー、Steam ライブラリカプセルなどの縦長プリセットは、PC表示時にプレビュー領域が縦に伸びますが、スマホ確認では問題ないため v1.1 では仕様として扱います。
- BOOTH / Ci-en / Skeb など一部は目安プリセットとして扱い、今後の実測や確認で調整します。
- 今後の候補として、サイト実表示風プレビューやカテゴリ別サービス選択UIを検討します。
